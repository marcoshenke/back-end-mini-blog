<%= form_with(model: kind_user) do |form| %>
  <% if kind_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(kind_user.errors.count, "error") %> prohibited this kind_user from being saved:</h2>

      <ul>
        <% kind_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
